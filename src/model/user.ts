import { BASE_URL } from "./base";

interface User {
  user_id: number;
  firstname: string;
  lastname: string;
  fullname: string;
  email: string;
}

const loginURL = BASE_URL + "login"

const actionLogin = () => {

}
